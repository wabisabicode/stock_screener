<!DOCTYPE html>
<html>
<head>
	<title>Stock Data Results</title>
</head>
<body>
	<h1>Results:</h1>
	<table>
		<tr>
			<th>Name</th>
			<th>Equity Ratio</th>
			<th>Net Debt / EBITDA</th>
			<th>AV Inv to Rev</th>
			<th>Inv to Rev MRQ</th>
			<th>As of Date</th>
		</tr>
		{% for item in data %}
		<tr>
			<td>{{ item.name }}</td>
			<td>{{ item.equity_ratio }}</td>
			<td>{{ item.net_debt }}</td>
			<td>{{ item.av_inv_to_rev }}</td>
			<td>{{ item.inv_to_rev_mrq }}</td>
			<td>{{ item.asOfDate }}</td>
		</tr>
		{% endfor %}
	</table>
</body>
</html>
